import React from "react";
import { Container, Navbar } from "react-bootstrap";

const Topbar = ()  => {
  return (
    <Container fluid>
        <Navbar />
    </Container>
  );
}

export default Topbar;
